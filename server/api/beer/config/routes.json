{
  "routes": [
    {
      "method": "GET",
      "path": "/beers",
      "handler": "Beer.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/beers/count",
      "handler": "Beer.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/beers/:_id",
      "handler": "Beer.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/beers",
      "handler": "Beer.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/beers/:_id",
      "handler": "Beer.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/beers/:_id",
      "handler": "Beer.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
